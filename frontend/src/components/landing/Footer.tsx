const Footer = () => {
  return (
    <footer className="border-t py-8 text-center text-gray-500">
      Distributed URL Shortener &copy; {new Date().getFullYear()}
    </footer>
  );
};

export default Footer;
